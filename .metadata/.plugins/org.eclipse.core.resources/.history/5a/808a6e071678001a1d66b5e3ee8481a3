package com.example.servingwebcontent;

import java.util.List;

import com.example.servingwebcontent.a_user;
import com.example.servingwebcontent.account;
import com.example.servingwebcontent.transaction;
import com.example.servingwebcontent.loan;

public interface Dao {

public void adda_user(a_user user);

public void deletea_user(a_user user);

void executeUpdatea_user(a_user user);

void addAccount(a_user user, account account);

void despositMoney(account account, transaction transaction);

void withdrawMoney(account account, transaction transaction);

void giveLoan(account account, loan loan);

}